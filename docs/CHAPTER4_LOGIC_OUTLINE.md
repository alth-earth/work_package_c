# 第四章 第二阶段产物：逻辑摘要、目录建议、框图、论点-证据矩阵、已有/缺失清单

> 依据 `docs/tmp/第四章论文编写总约束.md` §十三 工作流第二阶段输出。本文件为第四章正文写作的前置设计材料，正文写入 `docs/CHAPTER4.docx`。

---

## 1. 第四章逻辑摘要（约 500 字）

**研究对象**：北极航道船舶在时变冰—浪—风耦合环境下的动态气象导航方法。研究对象为"风险约束下的时空航路优化算法"及其在弱通信条件下的边缘决策支撑机制。

**核心科学/技术问题**：传统最短路径方法以几何距离最短为优化目标，忽视了冰—浪—风对船舶有效航速的影响，且静态风险图无法反映船舶抵达某网格时该网格的预测风险。如何在时变预测风险约束下，构造同时考虑航程、航行时间、环境风险、航速损失与不确定性的综合航路代价模型，并在弱通信条件下提供可执行的边缘决策摘要，是本章要解决的核心问题。

**数据输入**：第三章输出的多时效风险预测产品——含 `risk_mean`（连续风险代价）、`risk_level`（风险等级）、`hard_mask`（不可航行区域）、`uncertainty`（预测可信度）、`reason_code`（主要风险来源），以及多源环境观测（海冰密集度、冰厚、冰漂、风浪流、能见度、水深、通航密度）。本章使用的真实数据为北极冬航窗口的 145 帧风险预报序列（holdout 2026-02-22 与 development 2026-03-22 两独立窗口）。

**核心方法**：本章构建风险-速度-ETA 三方耦合的不动点边代价模型，将"船舶到达某网格时的预测风险"映射到该航段的可达速度与航行时间，再以综合航路代价（距离+时间+风险+航速损失+不确定性）驱动风险约束航迹规划。算法层面，本文采用时间依赖 A\* 算法在时间展开状态图上搜索，并以 admissible 启发式压缩搜索空间。

**模块数据流与因果逻辑**：多源环境与状态输入 → 时空状态构建 → 风险预测 → 风险代价场（含 hard_mask 与 uncertainty）→ 风险约束航迹规划（产出多模式航路与多时效候选方案）→ 在线重规划与控制接口 → 仿真验证。其中"误差反馈 → 风险场更新 → 规划参数修正"构成闭环。

**输出结果**：多模式气象航路（安全优先 / 效率优先 / 均衡推荐）+ 事件触发的动态重规划建议 + 弱通信条件下的关键风险摘要。

**验证方法**：以无信息 Dijkstra、静态场 A\*、风险无关基线为对照，在真实 145 帧风险预报序列与合成 4 档规模上进行配对比较；以消融研究验证各模块贡献；以反向论证验证改进候选未超越当前实现。

**本章最终需要证明的结论**：(1) 在真实北极 145 帧冰情上，相比静态场规划，本文方法的最大航段风险降低 15.3%–29.8%、平均航段风险降低 10.7%–16.5%；(2) 相比无信息 Dijkstra，扩展状态数减少 55.7%–86.6%、加速 2.16×–7.65×，且总代价严格相同，证明启发式加速未牺牲最优性；(3) 真实北极海洋数据违反 FIFO 性质（两窗口 43 500 / 40 776 次 interval 级违反），证明传统时依赖最短路支配剪枝在真实数据上不安全，本文采取"先证明、后启用"的保守工程纪律。

---

## 2. 第四章目录建议

按"科学问题 → 机理分析 → 方法设计 → 模型/算法构建 → 实验设计 → 定量评价 → 基线对比 → 消融/鲁棒性 → 反向论证 → 本章小结"组织。

```
第 4 章 风险约束动态气象导航与边缘决策方法

4.1 问题描述与总体技术框架
    4.1.1 北极航行动态气象导航的科学问题
    4.1.2 总体技术框架与数据流

4.2 冰—浪—风作用下船舶有效航速与航行代价模型
    4.2.1 环境对有效航速的机理影响
    4.2.2 航段航行时间与有效航速
    4.2.3 综合航路代价函数

4.3 预测风险约束的时空航路优化模型
    4.3.1 时空耦合风险 R(x, y, t_arrive)
    4.3.2 风险-速度-ETA 三方耦合不动点边代价
    4.3.3 硬约束条件与临时硬约束
    4.3.4 不同风险偏好下的目标函数权重

4.4 时间依赖 A* 算法与多模式气象航路生成
    4.4.1 时间展开状态图上的 A* 搜索
    4.4.2 admissible 启发式与最优性保证
    4.4.3 多模式航路（安全/效率/均衡）与多时效层级（全航程/24—72h/0—24h/0—6h）

4.5 事件触发动态重规划与弱通信边缘决策
    4.5.1 事件触发机制与触发阈值
    4.5.2 关键风险摘要与边缘信息压缩
    4.5.3 弱通信条件下的导航决策接口

4.6 仿真实验设计
    4.6.1 实验目的与对比对象
    4.6.2 数据集与实验变量
    4.6.3 算法公平性与实验统一设置

4.7 算法性能对比结果与分析
    4.7.1 搜索效率对比（vs 无信息 Dijkstra）
    4.7.2 航线质量对比（vs 静态场规划）
    4.7.3 可扩展性分析（合成规模曲线）
    4.7.4 效率-质量二维权衡

4.8 风险分布与逐段风险时序分析
    4.8.1 真实 24h 逐段风险时间序列
    4.8.2 风险分布箱线分析

4.9 消融、鲁棒性与反向论证
    4.9.1 模块消融研究（full / no_risk / no_heuristic / no_temporal）
    4.9.2 改进候选门禁反向论证
    4.9.3 诚实性边界与本章结论适用范围

4.10 本章小结
```

---

## 3. 第四章结构框图

```
┌─────────────────────────────┐
│ 多源环境与状态输入           │
│ (海冰/风浪流/能见度/水深)   │
└──────────────┬──────────────┘
               ▼
┌─────────────────────────────┐
│ 时空状态构建                 │
└──────────────┬──────────────┘
               ▼
┌─────────────────────────────┐    ┌──────────────────────┐
│ 风险预测模型                 │───▶│ 误差反馈              │
└──────────────┬──────────────┘    └──────────┬───────────┘
               ▼                              │
┌─────────────────────────────┐    ┌──────────▼───────────┐
│ 风险代价场                   │───▶│ 风险场更新            │
│ (risk_mean/level/mask/unc)  │    └──────────┬───────────┘
└──────────────┬──────────────┘               │
               ▼                              │
┌─────────────────────────────┐    ┌──────────▼───────────┐
│ 风险约束航迹规划             │───▶│ 规划参数修正          │
│ (时空耦合/三方耦合不动点/A*) │    └──────────────────────┘
└──────────────┬──────────────┘
               ▼
┌─────────────────────────────┐
│ 在线重规划与控制接口         │
│ (多模式/事件触发/边缘摘要)   │
└──────────────┬──────────────┘
               ▼
┌─────────────────────────────┐
│ 仿真/实测验证                │
└─────────────────────────────┘
```

> 框图使用论文级技术名称（"风险预测"、"时空耦合"、"三方耦合不动点"、"风险约束航迹规划"），不出现工作包/runner/script 等工程术语。蓝色实线为主链路，红色虚线为反馈闭环。对应实物图为 `fig-framework.png`。

---

## 4. 论点—证据—图表矩阵

| 论点 | 指标 | 对比对象 | 数据集 | 图 | 表 | 原始 CSV | 实验配置 | 结论边界 |
|---|---|---|---|---|---|---|---|---|
| 算法框架完整闭环 | 整体性 | — | 描述性 | 图4-1 框架 | — | — | — | 仅框架示意，不证明性能 |
| 比较公平 | 公平性 | 4 算法 | 全部 | — | 表4-1 公平性 | — | `benchmark_algorithm_comparison.py` | 仅"实验设置一致"，非性能结论 |
| 搜索效率更高 | 节点扩展数、运行时间、加速比 | 无信息 Dijkstra | synthetic 4 档 + 真实 2 窗口 | 图4-2 runtime-scale(log-log)、图4-3 runtime-cost、图4-4 runtime-risk | 表4-2 效率对比 | `summary-data.csv` | `--real-segment rolling_0_24h --repetitions 3` | 保持路径代价一致的条件下成立 |
| 航线质量更好 | 平均风险、最大风险 | 静态场规划 | 真实 24h 2 窗口 | 图4-5 风险时序、图4-6 风险分布、图4-7 质量柱状 | 表4-3 质量对比 | `summary-data.csv` raw JSON | 同上 | n=2 真实窗口，development 上三目标同路线 |
| 可扩展性 | 绝对扩展差值、加速比 | 无信息 Dijkstra | synthetic 4 档 | 图4-2 | — | 同上 | `--synthetic-profile {small,medium,large,stress}` | 百分比已饱和，用绝对差值 |
| 风险感知必要性 | 风险/时间/航程权衡 | 风险无关基线 | 真实 24h 2 窗口 | （图4-5、4-6 内含） | 表4-4 消融 | raw JSON | `--algorithm risk_blind` | n=1 有效（development=0%） |
| 改进候选未超越 | 候选评估结果 | 改进候选 | SSOT §3 | 图4-8 漏斗 | 表4-5 候选评估矩阵 | SSOT 引用 | — | "未被超越"≠"性能最优" |
| 真实数据违反 FIFO | interval 级负跳变计数 | FIFO 假设 | 真实 145 帧 | — | 表4-6 FIFO 反例 | SSOT 引用 | M24 分支 §P0.2-M1.12 | 改变了算法设计，非性能证据 |

---

## 5. 已有证据清单

### 5.1 已生成的图（9 类 × 中英双语 × PNG+SVG）

位于 `/root/my_project/.runtime/experiments/c-algorithm-comparison-summary/figures/`：

| 文件 | 论文图号 | 用途 |
|---|---|---|
| `fig-framework.png` | 图4-1 | 总体技术框架 |
| `fig-runtime-scale-log.png` | 图4-2 | 规模—运行时间（双对数） |
| `fig-runtime-cost.png` | 图4-3 | 运行时间—总代价散点 |
| `fig-runtime-risk.png` | 图4-4 | 运行时间—最大风险散点 |
| `fig-risk-timeseries.png` | 图4-5 | 真实 24h 逐段风险时序 |
| `fig-risk-distribution.png` | 图4-6 | 风险分布箱线 |
| `fig-scaling-expansion.png` | 图4-7 | 规模—扩展数（线性，已有） |
| `fig-risk-comparison.png` | 图4-8 | 风险对比柱状（已有） |
| `fig-speedup.png` | 图4-9 | 加速比（已有） |
| `fig-funnel.png` | 图4-10 | 改进候选门禁漏斗（反向论证） |

### 5.2 已生成的表（5 张）

| 表号 | 文件 | 内容 |
|---|---|---|
| 表4-1 | `docs/CHAPTER4_FAIRNESS_TABLE.md` | 算法公平性与实验统一设置 |
| 表4-2 | 主报告 §3 | 效率对比（vs Dijkstra） |
| 表4-3 | 主报告 §4 | 质量对比（vs 静态场） |
| 表4-4 | `docs/CHAPTER4_ABLATION.md` | 消融研究 |
| 表4-5 | 主报告 §6.6 表 A | 改进候选评估矩阵（反向论证） |
| 表4-6 | 主报告 §6.3 表 | FIFO 反例（科学发现） |

### 5.3 原始实验构件（9 个，全部 `schema c.algorithm-comparison.v2`）

- 合成 4 档：`c-algorithm-comparison-synthetic-{small,medium,large,stress}/comparison.json`
- 真实 24h 2 窗口：`c-algorithm-comparison-{holdout,development}-24h/comparison.json`（4 算法齐全，含 per-step 字段）
- 真实 6h 2 窗口：`c-algorithm-comparison-{holdout,development}-6h/comparison.json`（诚实性对照）
- 汇总：`c-algorithm-comparison-summary/{summary-data.csv, summary-tables.md}`

---

## 6. 缺失实验/图表清单

以下 4 项**严格未实现**，仅声明缺失原因。**不虚构数据、不画虚假误差棒。**

### 6.1 风险场空间热力底图与路径地理空间叠加图

- 真实风险场仅在 commit 内以 digest 索引，独立 zst 文件不含网格坐标到经纬度的投影矩阵。网格为 31×11 抽象索引，路径节点仅 `(row, col)`。
- 强行画需反归一化 zst 帧、投影矩阵、坐标映射，三者均未暴露，违反"禁止编造不存在的数据"。
- 替代：图4-5 时序图用 `step_edge_risk_score` 逐段呈现风险沿航线的变化；图4-6 分布图直接呈现风险统计特征。

### 6.2 `no_replanning` / `no_correction` 独立消融档

- 正式 `plan()` 默认就是**单次规划**，replanning baseline 是单独验证产物，与 `plan()` 默认路径正交。
- 在正式 control 下不存在"再剥一层重规划"的独立语义。
- 消融表4-4 严格只列 4 档（full / no_risk / no_heuristic / no_temporal），备注"重规划 baseline 与 `plan()` 默认路径正交"。

### 6.3 参数敏感性研究

- `planner_config.time_bucket_minutes / edge_sample_count / max_risk_frame_gap_minutes` 等参数有配置开关，但**无系统化扫参实验数据**，SSOT 也未记录 sweep 矩阵。
- 构建敏感性需 4-5 个参数笛卡尔积扫描，每组合保留 raw 数据，预算超出本轮。
- 替代：在 §4.6 实验设计中声明"实验设置一致"。**第四章不画敏感性图**。

### 6.4 development 24h 上 `risk_blind` 与 `recommended` 路线完全相同

- development 窗口 risk 权重不起决定性作用，**真实数据特征**，非缺失。
- 图4-4/4-6 **明确标注 `dev / risk_blind ≡ recommended`**，避免读者误判 n=1 偏差。
- 报告中已声明 n=1。

---

## 7. 论文正文写作的注意事项（从约束文件提取）

1. **学术论文风格**：使用"本文构建……"、"实验结果表明……"、"由图4-X可知……"，禁止"团队完成了"、"工作包A/B/C"、"runner/script"。
2. **图表优先**：先给数据/图表 → 描述现象 → 量化差异 → 分析原因 → 局部结论。禁止先写"本算法显著优于"再找数据。
3. **图题在下，表题在上**；公式按 (4-1)、(4-2) 编号；所有图表必须在正文中被引用。
4. **公平性约束**：先检查输入/起终点/风险场/可行域/目标函数/终止条件/硬件/随机种子/先验信息/精度差异，再宣称性能优劣。
5. **数据诚实**：n=1 必须标注"该结果仅用于现象说明，不构成统计意义上的性能结论"；失败实验可作为反向论证/正确性边界/设计选择依据。
6. **本章小结约 400-600 字**：按"问题→方法→数据结果→核心发现→对下一章接口"组织，引用定量结果，不用"首先、其次、最后"罗列软件任务。
7. **结构图风格**：白底、深蓝标题、浅蓝/浅青/浅绿/浅黄填充、深蓝细边框、圆角矩形、蓝色虚线边界、明确方向箭头、一级模块突出、输出模块单独色块；不使用 3D、阴影、渐变、PPT 风格。
8. **算法结果图风格**：白底、坐标轴清晰、字号统一、单位完整、图例简洁、误差棒/置信区间/样本量 n、同一章不同图算法颜色/线型语义一致。

---

## 8. 与下一章的接口

本章输出的多模式气象航路、动态重规划建议与弱通信边缘摘要，为下一章（第五章，工作包 D 的可视化与决策展示）提供：
- 多模式航路候选（安全/效率/均衡）
- 事件触发的重规划触发时刻与建议动作
- 关键风险摘要（风险等级、风险来源、不可通行区域、时间有效性、可信程度、建议动作）

下一章基于这些输出进行人机交互设计与决策可视化。