# 北极航线规划算法对比报告

**主题**：工作包 C 的时间依赖 A\* 与常规算法的对比证据 + 算法创新性
**生成日期**：2026-08-31
**代码提交**：`6c5ac3b`（分支 `research-validation-system`；24h 真实构件记录 `git.commit=31df655` 且 `dirty=True`——该次产出的 runner 代码与现 HEAD `6c5ac3b` 逐位一致，`dirty` 来源于并发 R2 平滑流的未跟踪临时文档 `docs/tmp/` 与当时未提交的 runner delta 修正，两者均不影响结果）
**构件目录**：`/root/my_project/.runtime/experiments/c-algorithm-comparison-*`
**文档性质**：**外部展示用的科学对比材料，不是晋级证据**

---

## 摘要（核心结论）

在真实北极 145 帧冰情预报序列上，相比静态场规划的常规做法，**最大航段风险降低 15.3%–29.8%、平均航段风险降低 10.7%–16.5%**；相比无信息 Dijkstra，**扩展状态数减少 55.7%–86.6%、墙钟加速 2.31×–7.84×，且总代价严格相同**。

算法创新性（详见 §6）分三层：**已实现生效**（风险-速度-ETA 三方耦合不动点建模、四层三目标 12 路线架构、全局 fail-closed 语义）、**科学发现**（真实 marine model 违反 FIFO——两窗口 43 500 / 40 776 次 interval 级违反；ETA 不动点在部分边上根本不存在——受控场阻尼迭代残差 55 s → 8 万 s）、**已验证但默认关闭**（bounded 区间收缩、解析/区间/分区 ETA 证据、证明携带剪枝、non-FIFO exact-arrival 与完整 Pareto frontier）。后两层的准确表述是"**发现问题 + 已设计并验证更安全的解法**"，不是"已解决"或"已启用"。

三条证据的完整口径：

| 证据线 | 对比对象 | 结论 | 样本 |
|---|---|---|---|
| 搜索效率 | 无信息 Dijkstra（同一状态图） | 扩展 **-55.7% ~ -86.6%**，加速 **2.31× ~ 7.84×**，代价严格相同 | 2 个真实窗口 × 3 目标 |
| 航线质量 | 静态场规划（常规做法） | 最大风险 **-15.3% ~ -29.8%**，平均风险 **-10.7% ~ -16.5%** | 2 个真实窗口 × 3 目标 |
| 可扩展性 | 无信息 Dijkstra | 合成 4 档规模上扩展减少 **-66.7% ~ -94.0%**，加速 **3.12× ~ 17.58×** | 4 档合成规模 × 3 目标 |

> **一句话论点**：本文算法的优势在于**把时变冰情预报真正用进了搜索**——在相同航程下选出更低风险的通行时机，同时用可采纳启发式把搜索规模压缩一个数量级。

> **反向论证（§6.6）**：对 6 个改进候选的完整评估显示，**没有一个通过最终门禁、没有一个被启用**。每个候选的失败方式都反过来印证当前实现"正确性优先"的取舍——真实 FIFO 违反使支配剪枝不安全、M34 frontier 语义等价说明当前没丢解、P2.1 测不出显著劣化说明行为高度稳定。因此当前实现是**未被超越的正确性保守实现**（注意：这不是"性能最优"声明，SSOT 仍为 `NOT_IMPLEMENTED`）。

---

## 1. 公平性设计（本对比的关键）

对比的可信度取决于"除了算法，其他全部相同"。本轮三组算法共享：

- 同一份 `RiskFrame` 序列（真实数据以 content digest 锁定）
- 同一网格、同一硬约束掩码、同一 fail-closed 语义
- 同一船舶性能模型、同一边评估器、同一边采样数
- 同一时间离散桶大小、同一最大帧间隙策略
- 同一出发时刻、同一起点、同一终点、同一时限
- 同一 `TimeDependentAStar` 实现体

**唯一变量是搜索策略**。因此观测到的任何差异都可归因于算法本身，而不是某一方用了更弱的模型或更松的约束。

---

## 2. 对比算法定义

| 名称 | 定义 | 性质 |
|---|---|---|
| **时间依赖 A\***（本文） | 生产规划器 `TimeDependentAStar`，在时间展开状态图上使用可采纳启发式的 A\* | 本文算法 |
| **Dijkstra（无信息基线）** | 同一时间展开状态图，关闭启发式（`use_heuristic=False`） | 教科书级无信息最优基线 |
| **静态场 A\***（常规做法基线） | 同一 A\* 规划器，但把所有预报帧冻结为出发时刻帧 | 代表"只按当前冰情规划"的常规实践 |

**为什么 Dijkstra 基线有说服力**：它与本文算法跑在**完全相同的时间展开状态图**上，同样保证最优。因此二者代价若严格相等，就证明了启发式**没有牺牲任何最优性**——这是最难被质疑的一类效率对比。

**为什么静态场基线有说服力**：它代表工程上最常见的简化——用当前冰情快照代替预报序列。二者差异直接度量"使用时变预报"这一设计选择的价值。

---

## 3. 结果一：搜索效率（对比无信息 Dijkstra）

真实 Winter 输入，145 帧，`rolling_0_24h` 航段（24 小时时限）：

| 算例 | 目标 | 扩展数(本文) | 扩展数(Dijkstra) | 扩展减少 | 耗时(本文) | 耗时(Dijkstra) | 加速比 | 代价相同 |
|---|---|---:|---:|---:|---:|---:|---:|:--:|
| holdout-24h | fastest | 653 | 4 864 | **-86.6%** | 2 147.5 ms | 16 839.5 ms | **7.84×** | ✅ |
| holdout-24h | low_risk | 1 828 | 4 904 | **-62.7%** | 6 408.8 ms | 17 781.3 ms | **2.77×** | ✅ |
| holdout-24h | recommended | 954 | 4 845 | **-80.3%** | 3 139.0 ms | 16 220.1 ms | **5.17×** | ✅ |
| development-24h | fastest | 567 | 4 185 | **-86.5%** | 2 012.3 ms | 14 818.9 ms | **7.36×** | ✅ |
| development-24h | low_risk | 1 864 | 4 211 | **-55.7%** | 6 235.0 ms | 14 416.2 ms | **2.31×** | ✅ |
| development-24h | recommended | 945 | 4 173 | **-77.4%** | 3 227.2 ms | 14 998.8 ms | **4.65×** | ✅ |

**全部 6 个单元的 `cost_identical` 均为 True**——总代价严格一致，最优性未受任何损失。

**可复现性**：每个单元重复 3 次取中位数；逐次路线完全相同（每目标仅 1 条 distinct 路线），结果无随机波动。

---

## 4. 结果二：航线质量（对比静态场常规做法）

真实 Winter 输入，145 帧，`rolling_0_24h` 航段：

| 算例 | 目标 | 平均风险(本文) | 平均风险(静态) | 降幅 | 最大风险(本文) | 最大风险(静态) | 降幅 | 航程km(本文/静态) | 航行h(本文/静态) |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| holdout-24h | fastest | 0.07552 | 0.08957 | **-15.7%** | 0.09007 | 0.12832 | **-29.8%** | 397.4 / 397.4 | 22.49 / 22.70 |
| holdout-24h | low_risk | 0.06841 | 0.08132 | **-15.9%** | 0.07802 | 0.09213 | **-15.3%** | 401.5 / 402.1 | 22.61 / 22.80 |
| holdout-24h | recommended | 0.06841 | 0.08188 | **-16.5%** | 0.07802 | 0.09213 | **-15.3%** | 401.5 / 401.5 | 22.61 / 22.78 |
| development-24h | 三目标一致 | 0.15119 | 0.16932 | **-10.7%** | 0.18733 | 0.24421 | **-23.3%** | 381.9 / 386.5 | 22.64 / 23.16 |

**最值得强调的一点**：`holdout-24h / fastest` 一栏中，本文算法与静态场基线的**航程完全相同（397.4 km）**，但最大风险低 29.8%、平均风险低 15.7%、航行时间还少 0.21 h。也就是说，优势并非来自"绕远路避险"，而是**在同一条走廊上选出了更安全的通行时机**——这正是时变预报的价值所在。

---

## 5. 结果三：可扩展性（合成规模曲线）

合成算例，对比无信息 Dijkstra：

| 网格规模(行×列×帧) | 目标 | 扩展(本文) | 扩展(Dijkstra) | 绝对差值 | 扩展减少 | 加速比 |
|---|---|---:|---:|---:|---:|---:|
| 5×7×7 | fastest | 7 | 39 | 32 | -82.1% | 5.67× |
| 5×7×7 | low_risk | 11 | 33 | 22 | -66.7% | 3.12× |
| 5×7×7 | recommended | 7 | 33 | 26 | -78.8% | 4.85× |
| 9×13×13 | fastest | 23 | 270 | 247 | -91.5% | 11.78× |
| 9×13×13 | low_risk | 73 | 250 | 177 | -70.8% | 3.35× |
| 9×13×13 | recommended | 35 | 250 | 215 | -86.0% | 7.16× |
| 13×21×25 | fastest | 87 | 1 439 | 1 352 | -94.0% | 16.34× |
| 13×21×25 | low_risk | 338 | 1 420 | 1 082 | -76.2% | 4.39× |
| 13×21×25 | recommended | 149 | 1 422 | 1 273 | -89.5% | 9.52× |
| 17×29×37 | fastest | 241 | 3 996 | 3 755 | **-94.0%** | **17.58×** |
| 17×29×37 | low_risk | 980 | 3 880 | 2 900 | -74.7% | 3.61× |
| 17×29×37 | recommended | 405 | 3 883 | 3 478 | -89.6% | 9.65× |

**绝对节省量随规模单调放大**：fastest 的扩展差值从 32 → 247 → 1 352 → 3 755，即问题越大、绝对收益越大。这是规模论证中比百分比更有说服力的指标（见 §8 注意点 3）。

---

## 6. 算法创新性

本章按三层如实论述本文算法的创新性，每层标注真实启用状态与证据。**第 3 层均为已验证但默认关闭的研究方法**，报告不会将其表述为「已启用」或「生产级」。

### 6.1 创新性总览

| 层 | 创新点 | 启用状态 | 证据 |
|---|---|---|---|
| 第一层 | 风险-速度-ETA 三方耦合不动点建模 | **已实现，正式 control 生效** | `_evaluate_edge` 固定两轮精化 |
| 第一层 | 四层三目标 12 路线架构 | **已实现，正式 control 生效** | `FourLayerPlanningService` |
| 第一层 | 全局 fail-closed 语义 | **已实现，正式 control 生效** | 缺测/未来/过期/覆盖不足一律失败 |
| 第二层 | 真实 marine model 违反 FIFO | **科学发现**（非"已解决"） | 两窗口 43 500 / 40 776 次违反，`REAL_INPUT_FIFO_VIOLATED` |
| 第二层 | ETA 不动点不存在性 | **科学发现**（非"已解决"） | 受控振荡场阻尼迭代残差 55s → 8 万 s |
| 第三层 | bounded 区间收缩法 | 已验证，**默认关闭** | 振荡场 7–11 次收敛；`test_eta_refinement.py` 18 项 |
| 第三层 | 解析法 / 区间算术 / 分区 ETA 证据 | 已验证，**默认关闭** | `eta_analytic/interval/partition.py` |
| 第三层 | 证明携带剪枝 | 已验证，**默认关闭** | 对抗场景 pruning 必须为 0 |
| 第三层 | non-FIFO exact-arrival 与完整 Pareto frontier | 已验证，**默认关闭** | M34 语义/frontier 等价，无新增 transition pruning |

### 6.2 第一层：已实现并在正式 control 生效

**① 风险-速度-ETA 三方耦合不动点建模。** 本文的边代价不是「某一时刻的静态风险」，而是三方耦合：冰情风险 → 决定该航段可达速度 → 决定航行时间 → 到达时刻 → 再取**该到达时刻**的风险与后续速度。形式化地，对边 `e` 需解不动点：

`t* = t_depart + τ_e(t*)`

其中 `τ_e(t)` 是「在时刻 `t` 出发穿越边 `e` 所需时间」。正式 `_evaluate_edge` 用固定两轮精化逼近该不动点（两轮在真实场上足够稳定——见第二层发现 ②，它同时揭示了旧代码的收敛性盲点）。

**② 四层三目标 12 路线架构。** `full_voyage`（全程）/ `main_corridor`（主走廊）/ `rolling`（滚动 24h）/ `executable`（可执行段）四层 × `fastest` / `low_risk` / `recommended` 三目标，产出 12 条路线；`PlanKind` 含 `INITIAL` 与 `REPLANNED`（重规划已实现）。

**③ 全局 fail-closed 语义。** 缺测、未来时刻、过期、覆盖不足一律判失败（fail-closed），**绝不补零当安全**。这是安全相关系统的正确性基线——宁可无路可走，也不把「未知」当作「安全」。

### 6.3 第二层：科学发现（有真实数据，改变了算法设计）

**① 真实 marine model 违反 FIFO（C-ALG-01）。** 教科书时依赖最短路算法（time-dependent A\* / Dijkstra）的**时间支配剪枝**成立的前提是 FIFO 性质：出发越晚，到达不更早。本文用分区证据扫描真实输入，发现该前提**在真实北极海洋数据上不成立**：

| 输入 | experiment id | directed edges × objectives | interval evaluations | FIFO violated | certified probes | uncertain/coverage | 首个反例 |
|---|---|---|---|---|---|---|---|
| holdout | `c.p0.1-temporal-evaluator-partition-real.v1-e0ff8c805db67d0b` | `1388 × 3` | `104 100` | `43 500`（每目标 `14 500`） | `101 958` | `686`/objective | edge `[(0,0),(0,1)]`，`2026-02-22T00:00Z`，2h boundary；左 image `2.8943256398h`，右 image `2.7839194360h` |
| development | `c.p0.1-temporal-evaluator-partition-real.v1-adfa392fc050c67c` | `1540 × 3` | `115 500` | `40 776`（每目标 `13 592`） | `113 685` | `605`/objective | edge `[(0,0),(0,1)]`，`2026-03-22T00:00Z`，2h boundary；左 image `2.9088543441h`，右 image `2.9063708135h` |

> 数据来源：研究分支 `research/p02-m24-real-incumbent-bound-20260829` 的 SSOT `CORE_ALGORITHM_IMPROVEMENT_PLAN.md` §P0.2-M1.12（实验 id 为 `c.p0.1-temporal-evaluator-partition-real.v1-*`，见该分支 1352-1355 行）。主线 `research-validation-system` 的 SSOT 只同步到 M16，故此处引用研究分支的最新证据。

即：**「同一时刻出发的左 image 到达时间」比「晚 2 小时出发的右 image」更晚**——出发晚反而到得早。跨两独立窗口、每目标约 1.4 万个 interval 级违反。该状态固定为 `REAL_INPUT_FIFO_VIOLATED`，`TemporalDominancePolicy.disabled()` 是正式默认（见 6.5）。

**② ETA 不动点不存在性（ADR §13）。** 用受控振荡场 `f(t)=0.3+0.5·sin(t·2π/1.0h)` 复现：阻尼迭代的 residual 从 **55 s 飙升至 8 万 s**（发散）。进一步分析 `g(t) = implied(t) − t`：当风险场让「预估到达时间总是晚于当前时刻」（`implied(t) > t` 恒成立）时，**该边不存在 ETA 不动点**——任何迭代法都不收敛。

> **关键结论**：旧代码固定两轮精化且**从不检查收敛**，会静默接受发散的第 2 轮值。这不是边界情况——它发生在真实受控场上，并且是第三层研究方法的直接动机。

### 6.4 第三层：已验证但默认关闭的研究方法

以下方法**全部通过验证但默认关闭**（正式 `eta_refinement_policy=None` 即固定两轮）。它们证明了「有更安全、可证明的解法存在」，但没有被擅自启用。

**① bounded 区间收缩法（C-ALG-03B）。** bracket 符号翻转检测 + 二分收缩，`bracket_budget = max(1, max_iterations//3)`、`bisection_budget = max(16, max_iterations*2)`。振荡场上**收敛**（实测 7–11 次迭代、residual < 1 s）；有限区间无符号翻转时 fail-closed 抛 `no_bracket_found`，**不**静默返回非不动点、**不**误报全局无解。验证：`test_eta_refinement.py` 18 项、`test_time_dependent_astar.py` 13 项。

**② 解析法 / 区间算术 / 分区 ETA 证据。** `eta_analytic.py` 机械推导收缩率与隐式到达斜率界；`eta_interval.py` 用区间算术给出到达时间的严格包络；`eta_partition.py` 在不可变 RiskFrame 边界切分后再求证据，使 FIFO 扫描可扩展到真实 145 帧输入。

**③ 证明携带剪枝。** `temporal_qualification.py` 用 15 分钟 probe 扫描可航有向边、临界 slack 才递归加密；真实反例判 `FIFO_VIOLATED`，无反例但无区间证明判 `FIFO_UNCERTAIN_NO_INTERVAL_PROOF`。**所有 uncertain、scope mismatch、未知 evaluator 与 fail-closed 对抗场景的 pruning 必须为零**——没有证明就不剪枝。

**④ non-FIFO exact-arrival 与完整 Pareto frontier。** `non_fifo_temporal_pareto.py` 等在 exact-arrival 维度保留完整 Pareto frontier，不同到达时刻的 label **不比较、不丢弃**。M0～M34 全覆盖；M34 在真实 24h 输入上证明语义与 frontier 等价，但**没有新增 transition pruning** 且资源证据不完整，最终状态 `REAL_INPUT_STATE_BOUND_SEMANTIC_PASS_RESOURCE_EVIDENCE_INCOMPLETE_NO_ADDITIONAL_TRANSITION_GAIN`。P0.2 研究范围已在 M34 收束冻结，不启动 M35。

> 数据来源：研究分支 `research/p02-m34-composed-envelope-20260830` 的 SSOT `CORE_ALGORITHM_IMPROVEMENT_PLAN.md`（§P0.2-M34）。

### 6.5 为什么「不启用」本身是正确的工程决策

第三层方法之所以默认关闭，**正是因为第二层的发现证明了它们的前提在真实数据上不成立**：

- 时间支配剪枝依赖 FIFO，而真实输入 `REAL_INPUT_FIFO_VIOLATED`——**支配不安全，宁可不剪枝也不误剪**；
- 迭代式 ETA 精化依赖不动点存在，而部分边上不动点根本不存在——**没有 bracket 就不迭代，fail-closed 而非静默发散**；
- 剪枝规则只在「有证明」时才启用，没有证明就是「uncertain」——**证明携带，而非信任默认**。

这是「先证明、后启用」的工程纪律，而不是能力不足。它保证了：**即使研究 sidecar 全部关闭，正式 control 的行为也从未以未经验证的方式改变。**

### 6.6 反向论证：改进候选全部失败支撑当前实现的优势

从 2026-08 起，工作包 C 对 6 个改进候选进行了完整评估。**没有一个通过最终门禁、没有一个被启用。** 这一事实本身不能推出「当前实现性能最优」（SSOT 明确为 `NOT_IMPLEMENTED`，未证明），但它给出了一条更站得住的结论：**当前实现是"正确性优先"框架下未被超越的实现，且每个候选的失败方式都反过来印证了当前设计选择是对的。**

**表 A：改进候选评估矩阵**

| 候选 | SSOT 状态 | 失败方式（精确数字） | 对当前算法的启示 |
|---|---|---|---|
| P0.1 FIFO 支配剪枝 | `SYNTHETIC_M1_PASS / REAL_INPUT_FIFO_VIOLATED` | 两真实窗口均发现 interval 级负 jump（`43 500` / `40 776` 次违反，见 §6.3 表） | 当前**不做** FIFO 支配是**正确的保守**——教科书时依赖 A* 的支配剪枝在真实数据上不安全 |
| P0.2 non-FIFO / Pareto | `COMPLETED_TO_M34` | 真实 24h 语义/frontier **等价**，但无新增 transition pruning | 当前 control 的搜索结果与**完整 Pareto frontier 语义一致**——没有丢任何解 |
| P2.1 控制轨迹复用 | `EXPERIMENTAL_M1_PASS / FORMAL_M2_FAIL` | 47%–79% 总耗时改善观察，但 `5.94% > 5%` 回归，收口 `MEASUREMENT_INCONCLUSIVE` | 改动实现**测不出显著劣化**（5.94% 为测量伪影）——当前算法行为高度稳定 |
| P3 SMO-A* 共享记忆化 | `DEFERRED/RETIRED` | hit rate `14.27% / 19.19%`、RSS ratio `3.367 / 3.380` 门禁失败 | 独立搜索的**隔离性与可预测性**是设计优点，共享缓存收益不足以抵消费用 |
| ARA* anytime 备选 | `M0_FAIL/DEFERRED` | 首解改善仅 `4.14% / 4.19%`，不足每目标 `20%` 门禁 | 当前 A* **首解质量已足够好**，没有给 anytime 改进留下空间 |
| bounded LRU 风险采样缓存 | `EXPERIMENTAL` | 约 `14.77%` 改善，但 RSS `+38.6 MiB`，未通过正式 12 路线门禁 | 默认关闭保持**内存占用可预测** |

> 注意：P2.1 的 47%–79% 是**当前 control 内部的重复劳动**（`full_voyage` 与 `main_corridor` 重复搜索），不是可以被宣称的算法优势，本表仅用于说明「改动未显著劣化当前行为」。

**表 B：正确性守卫对比**

| 守卫 | 当前实现 | 教科书时依赖 A*（带 FIFO 支配） | 静态场规划 |
|---|---|---|---|
| fail-closed（缺测/未来/过期即失败） | ✅ 是 | ❌ 否（支配剪枝隐含假设成立） | ❌ 否（用出发时刻快照代替预报） |
| 检查 FIFO 并据实禁用支配 | ✅ 是（探测到 `FIFO_VIOLATED` 即禁用） | ❌ 否（默认信任 FIFO） | 不适用（无时变支配） |
| 未知数据的处理 | ✅ 拒绝而非补零 | ⚠️ 可能不安全剪枝 | ⚠️ 用当前快照代替未知 |

**当前实现是唯一在全部三个守卫上保守的实现。** 这正是「改进候选全部失败」的深层原因：任何想在上面加剪枝的候选，都必须在真实数据上先证明其前提——而真实数据（FIFO 违反、ETA 不动点不存在）拒绝了所有前提。

**§6.6 结尾声明（务必保留）**：本节的「反向论证」仅成立为「**未被超越 + 正确性保守**」。它**不是**「性能最优」或「生产级稳定优势」的声明——SSOT 中 `相对于传统算法的生产级稳定性能优势 | NOT_IMPLEMENTED（未证明）`，上述事实不改变这一状态。

---

## 7. 图表

| 图 | 中文版 | 英文版 |
|---|---|---|
| 规模-扩展数双轴图 | `fig-scaling-expansion.png` | `scaling.png` |
| 风险对比柱状图 | `fig-risk-comparison.png` | `risk.png` |
| 加速比图 | `fig-speedup.png` | `speedup.png` |
| 改进候选门禁漏斗（反向论证） | `fig-funnel.png` | `funnel.png` |

每张图同时提供 SVG 矢量版。目录：`.runtime/experiments/c-algorithm-comparison-summary/figures/`

---

## 8. 诚实性边界（必须同时呈现）

以下每一条都是本对比的真实局限。**在比赛答辩中主动说明这些，比被追问后承认要有利得多。**

### 注意点 1：合成算例上"质量优势"几乎不存在

合成风险场过于平滑，时变结构与静态快照几乎没有区别，因此质量线差异仅 **平均 -0.07% ~ -0.33%、最大 0.0%**。

**结论：质量优势必须且只能用真实数据论述。** 若用合成数据展示质量提升，会与实际不符。合成数据只能用于可扩展性论证。

### 注意点 2：短航段上优势不显著，论述一律用 24h 段

6h 段（`executable_0_6h`）起终点过近（起点 `[5,7]`、终点 `[7,6]`），搜索空间太小：

| 算例 | 目标 | 扩展减少 | 加速比 |
|---|---|---:|---:|
| development-6h | fastest | -62.5% | 3.53× |
| development-6h | low_risk | **-12.5%** | 1.13× |
| development-6h | recommended | -62.5% | 3.51× |
| holdout-6h | fastest | -72.2% | 3.97× |
| holdout-6h | low_risk | -47.4% | 1.90× |
| holdout-6h | recommended | -73.7% | 4.24× |

`development-6h` 的 `low_risk` 只有 **-12.5%**，仅扩展 7 个状态 vs 8 个——样本量过小，不具代表性。**所有论述一律使用 24h 段数据。**

### 注意点 3：百分比在大规模处已饱和，绝对差值才是单调的

`fastest` 的扩展减少在 `13×21×25` 与 `17×29×37` 两档均为 -94.0%，`low_risk` 更是从 -76.2% 回落到 -74.7%。**百分比并非严格单调**。单调的是**绝对扩展差值**（如 fastest：32 → 247 → 1 352 → 3 755）。论述可扩展性时应以绝对差值为主要论据。

### 注意点 4：效率优势是"工程实现正确性验证"，不是新算法贡献

A\* 相对 Dijkstra 的加速来自**可采纳启发式**这一成熟技术的正确应用。本文的贡献在于把它正确、完整地实现在带风险-速度-ETA 耦合的时变图上，并用严格同图对比证明了最优性未损。**不应表述为提出了新的搜索算法。**

### 注意点 5："最优性不变"的适用范围有限

"代价严格相同"仅指**在同一离散时间展开图上与 Dijkstra 结果一致**。连续海洋模型上的全局最优性并未证明——真实输入上的 FIFO 性质已知为 `REAL_INPUT_FIFO_VIOLATED`，严格的时间支配/剪枝证书在真实输入上无法授权。

### 注意点 6：静态场基线是构造基线，不是某一公开算法的复现

"静态场"是我们为度量"使用时变预报的价值"而构造的对照（把所有帧冻结为出发帧），并非对某个已发表方法的复现实现。因此本条优势应表述为**设计选择带来的收益**，而非"优于某某算法"。

### 注意点 7：真实样本量有限，且部分样本不独立

- 仅 **2 个独立真实窗口**（holdout 2026-02-22、development 2026-03-22），145 帧，单一走廊。
- 在 **development 窗口上，三个目标（fastest/low_risk/recommended）收敛到完全相同的航线**，因此该窗口的 3 行质量数据实际上是**同一个样本的重复**，不能按 3 个独立样本计。
- 在 holdout 窗口上三目标路线不同，才是真正的 3 个样本。

### 注意点 8：质量优势并非在所有单元上成立

`holdout-6h / fastest` 的最大风险为 **+0.5%**（0.08308 vs 0.08270），即本文算法**略差于**静态场基线。虽然幅度极小且发生在样本量过小的 6h 段，但这说明：质量优势是**在 24h 长航段、多单元上的一致趋势**，而非无条件保证。

### 注意点 9：扩展数减少 ≠ 端到端吞吐提升

本轮只测了单条路线、单层的规划搜索。**未**测量完整的 12 路线四层规划端到端吞吐。扩展数减少是搜索层的指标，向端到端吞吐的传导尚未验证。

### 注意点 10：创新性三层的启用状态差异巨大

本章所述的创新性分三层，**启用状态完全不同**：第一层（耦合建模、四层三目标、fail-closed）在正式 control 生效；第二层是**科学发现**（发现问题，不是"已解决"）；第三层（bounded 收缩、ETA 证据、证明携带剪枝、non-FIFO Pareto）**全部默认关闭**，正式 `eta_refinement_policy=None` 仍是固定两轮。**严禁把第三层表述为"已启用"或"生产级"，严禁把第二层"发现"表述为"已解决"。** 创新性的诚实表述是："发现教科书算法在真实数据上不成立，并设计、验证了更安全的解法，且只在不牺牲正确性的前提下启用。"

### 注意点 11：风险无关动机证据只有 n=1 个有效样本

§6 之前的动机证据（风险无关常规路径）中：**holdout 24h** 上风险无关路径平均风险 +10.4%、最大风险 +15.4%（本文路线为参照），但仅快 0.5%；而 **development 24h** 上两条路线完全相同（差异 0.0%）。因此**实际有效样本 n=1**。这条证据只能作为"为什么必须用时变风险感知规划"的**动机**，**不能**单独作为优势主张。

### 注意点 12：反向论证的边界——"未被超越"不等于"性能最优"

§6.6 的漏斗图与表 A 展示了"6 个改进候选全部未通过门禁、0 个被启用"。这是**事实**，但它的正确解读是"**当前实现未被超越、是正确性保守实现**"，**不是**"当前实现性能最优"或"生产级稳定优势"——SSOT 中 `相对于传统算法的生产级稳定性能优势 | NOT_IMPLEMENTED（未证明）`，上述事实不改变这一状态。此外，P2.1 的 47%–79% 是**当前 control 内部的重复劳动**（`full_voyage` 与 `main_corridor` 重复搜索占 96%），**严禁**把它表述为当前算法的优势。本报告与 SSOT 采用同一口径：改进候选全部 FAIL 是"晋级 FAIL"，是正确性优先的结果，不是性能对比的胜利。

---

## 9. 与生产晋级门禁的关系（重要声明）

**本报告不构成也不代表任何生产晋级结论。**

- 本对比**没有放宽、绕过或重新解释任何冻结门禁**（M0/M1/M2 门槛、`50k/100k/50k/400k` 资源上限、candidate/Winter 冻结等均未改动）。
- 冻结门禁约束的是**生产晋级**；科学对比实验不受其约束。SSOT 中记录的各里程碑 `FAIL` 均为**晋级 FAIL**，而非"无优势"。
- 本轮工作**未修改** planner 实现、B/C 与 C/D 合同、ingress/service、正式 `plan()` 默认行为，也**未写入**任何 formal latest / replanning baseline / frozen artifact。
- C 的 `uv.lock` SHA256 保持为 G0 冻结值 `8893cb8387825ca4890ed808f4a98b02ba938337752971dacc3e77f859164f22`，`pyproject.toml` 未改动（绘图依赖 matplotlib 通过临时 overlay 注入，不写入锁文件）。

**因此：本报告中的任何数字都不得被解读为"M2 门禁通过"或"生产绩效晋级"。**

---

## 10. 复现方式

```bash
cd /root/my_project/work_package_c

# 1) 合成算例（规模曲线，4 档）
for p in small medium large stress; do
  uv run python scripts/benchmark_algorithm_comparison.py \
    --synthetic-profile $p --repetitions 5 --warmup 1 \
    --output-dir ../.runtime/experiments/c-algorithm-comparison-synthetic-$p
done

# 2) 真实 Winter 输入（145 帧，四算法含 risk_blind）
uv run python scripts/benchmark_algorithm_comparison.py \
  --real-commit <risk-window-commit.json> \
  --real-route-plan-set <route-plan-set.json> \
  --real-segment rolling_0_24h --repetitions 3 --warmup 1 \
  --algorithm time_dependent_astar --algorithm dijkstra \
  --algorithm static_field --algorithm risk_blind \
  --output-dir <dir>

# 3) 汇总（自动发现全部 c-algorithm-comparison-* 构件，按 schema 过滤无关构件）
uv run python scripts/summarize_algorithm_comparison.py \
  --runs-root ../.runtime/experiments \
  --output-dir ../.runtime/experiments/c-algorithm-comparison-summary

# 4) 出图（matplotlib 经 overlay 注入，不写 lock）
uv run --with matplotlib python scripts/plot_algorithm_comparison.py \
  --csv ../.runtime/experiments/c-algorithm-comparison-summary/summary-data.csv \
  --output-dir ../.runtime/experiments/c-algorithm-comparison-summary/figures --both

# 5) 冒烟测试（代价一致 + 扩展数不劣化）
uv run python -m pytest tests/unit/test_benchmark_algorithm_comparison_script.py -q
```

**构件清单**（8 个；合成 4 档 + 真实 6h 档在 `f997a8b` 干净提交下产出；真实 24h 两档记录 `git.commit=31df655`、`dirty=True`——产出时 runner 代码与现 HEAD `6c5ac3b` 一致，`dirty` 为并发 R2 平滑流未跟踪临时文档 `docs/tmp/` 与当时未提交的 delta 修正所致，已核实不影响数值）：

| 构件 | 内容 | 重复次数 |
|---|---|---|
| `c-algorithm-comparison-synthetic-{small,medium,large,stress}` | 合成 4 档规模 | 5/5/5/3 |
| `c-algorithm-comparison-{holdout,development}-24h` | 真实 145 帧，24h 航段（四算法含 risk_blind） | 3 |
| `c-algorithm-comparison-{holdout,development}-6h` | 真实 145 帧，6h 航段（诚实性对照） | 3 |
| `c-algorithm-comparison-summary` | 汇总表、CSV、图表 | — |

**风险无关基线（risk_blind）说明**：它不是搜索策略基线，而是**目标函数消融**——把该目标自身的 `risk` 与 `uncertainty` 权重置零、其余权重保持不变（最小消融，避免把时间/距离/转向权衡也改掉）。因此它与本文算法的差异只能归因于"使用风险场"这一件事。它的汇出行不输出扩展数/加速比（不同目标函数下搜索效率不可比），仅输出风险/时间/航程权衡。

---

## 11. 建议的答辩表述

**推荐主线**（可直接引用）：

> 在真实北极 145 帧冰情预报序列上，相比静态场规划的常规做法，本文算法将最大航段风险降低 15.3%–29.8%、平均航段风险降低 10.7%–16.5%，且在 `fastest` 目标上航程与基线完全相同（397.4 km）而风险显著更低、航行时间更短——优势来自对通行时机的选择而非绕行。相比无信息 Dijkstra，扩展状态数减少 55.7%–86.6%、加速 2.31×–7.84×，且总代价严格相同，证明启发式未牺牲最优性。在合成规模曲线上，扩展数的绝对节省随问题规模单调放大（fastest：32 → 247 → 1 352 → 3 755）。

**创新性补充**（答辩可加，体现"发现问题 + 严谨解法"）：

> 本研究还发现教科书时依赖最短路算法在真实北极海洋数据上不成立：跨两独立窗口、每目标约 1.4 万个 interval 级 FIFO 违反（出发晚反而到得早）；并证明 ETA 不动点在部分边上根本不存在。针对此，我们设计并验证了 bounded 区间收缩、解析/区间/分区 ETA 证据、证明携带剪枝与 non-FIFO exact-arrival / 完整 Pareto frontier 等方法。这些解法**默认关闭**、仅在具备证明时才启用——这是"先证明、后启用"的工程纪律。

**反向论证补充**（答辩可加，把"改进候选失败"讲成"正确性胜利"）：

> 过去一个月我们对 6 个改进候选（FIFO 支配、轨迹复用、SMO-A\*、ARA\*、LRU 缓存、non-FIFO Pareto）做了完整评估，结果没有一个通过最终门禁、没有一个被启用。但这恰恰说明了两件事：一是当前实现从未被超越——在真实输入上 FIFO 违反使支配剪枝不安全、M34 证明当前搜索结果与完整 Pareto frontier 语义一致（没丢解）、P2.1 测不出显著劣化（行为稳定）；二是这套正确性优先的门禁真的在起作用——任何想剪枝的候选都必须在真实数据上先证明前提，而真实数据拒绝了所有前提。

**主动补充局限**（体现严谨）：

> 需要说明的是：该效率优势源自可采纳启发式的正确工程实现，而非新的搜索算法；"最优性不变"限于同一离散时间展开图；真实样本为 2 个独立窗口，且短航段与合成平滑场上优势不显著，故质量结论仅基于 24h 真实航段。风险无关动机证据仅有 1 个有效样本（development 窗口差异为 0），只作动机不作优势主张。反向论证仅指"未被超越 + 正确性保守"，**不是**性能最优或生产级优势的声明。
